## 1. 首页模板修改

- [x] 1.1 在 `src/pages/index.astro` 的"开源项目" `.list` 容器内、ArcReel 条目之前，插入 AI Pulse Tab 的 `<a class="list-row">` 条目

## 2. 验证

- [x] 2.1 运行 `pnpm build` 确认构建无报错
- [x] 2.2 启动 `pnpm dev` 在浏览器中确认：AI Pulse Tab 显示在第一位、ArcReel 在第二位、链接正确跳转到 GitHub
